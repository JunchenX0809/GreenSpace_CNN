# Core deep learning - TensorFlow/Keras stack
# IMPORTANT:
# - We intentionally do NOT pin/install TensorFlow here because the correct package
#   depends on platform (Apple Silicon vs Linux/Windows vs Intel macOS).
# - See README "Install TensorFlow" section for the correct command.
# - Avoid pinning standalone `keras` separately; it can cause TF/Keras version skew.

# PyTorch experiment stack (used by src_torch and notebooks/03_torch_*)
# Pin torch + torchvision together (official matrix: 2.10.0 + 0.25.0).
# Use arm64 Python on Apple Silicon (/opt/homebrew/bin/python3.11); x86_64 venvs
# may not get these torch builds from PyPI.
# torchgeo pulls lightning, kornia, segmentation-models-pytorch, etc. — no need to pin those separately.
torch==2.10.0
torchvision==0.25.0
torchgeo==0.8.1
timm==1.0.27
torchmetrics==1.9.0

# Computer vision and image processing
opencv-python<4.12
pillow>=10.0.0
albumentations>=1.3.0
scikit-image>=0.21.0

# Geospatial (for satellite imagery)
rasterio>=1.3.0
geopandas>=0.13.0
shapely>=2.0.0
pyproj>=3.6.0

# Data handling and analysis
pandas>=2.0.0
numpy<2
scikit-learn>=1.3.0
h5py>=3.9.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Experiment tracking
tensorboard>=2.13.0
wandb>=0.15.0

# Jupyter ecosystem
jupyter>=1.0.0
ipywidgets>=8.0.0
nbformat>=5.9.0

# Utilities
tqdm>=4.65.0
pydantic>=2.0.0
pyyaml>=6.0
typer>=0.9.0

# Google Drive integration
pydrive2>=1.15.0
python-dotenv>=1.0.0
google-auth-oauthlib>=1.1.0

# Optional: For deployment
# fastapi>=0.100.0
# uvicorn>=0.23.0
